{
  "name": "welovemovies",
  "version": "1.0.0",
  "description": "Back-end for We Love Movies application",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsConfig": "npx tsc --init --rootDir src --outDir build/ --esModuleInterop --resolveJsonModule --lib es2023 --module commonjs --allowJs true --noImplicitAny true",
    "build": "npx tsc",
    "start": "node build/index.js",
    "dev": "node --watch -r ts-node/register --env-file=config.env src/app.ts"
  },
  "keywords": [
    "Movies",
    "Cinema",
    "NodeJs",
    "Typescript"
  ],
  "author": "Nguetseng Stephane",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}